//- ** This is a sample of a pug template and how it uses the data passed to it from index.js.

doctype html
html
    head
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body
        h1 Dev Info Custom Object Table
        table
            tr
                th Name
                th Address
                th Favorites
            each obj in data
                tr
                    td #{obj.properties.name}
                    td #{obj.properties.address}
                    td #{obj.properties.favriates}